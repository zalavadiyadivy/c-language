#include <stdio.h>

int main()
{
    int *p,i,n,a[20];
    printf("Enter Size: ");
    scanf("%d",&n);
    
    printf("Enter Elements:\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",*p*(*p));
    }
    p=&a;
    
    for(i=0;i<n;i++)
    {
        printf("%d",*p*(*p));
    }
    return 0;
}
