#include <stdio.h>
int main(){
   int a,b,c;
   printf("Enter two angles of triangle value:");
   scanf("%d %d",&a,&b);
   c=180-(a+b);
   printf("\n Third angle of the triangle value is : %d",c);

   
}
